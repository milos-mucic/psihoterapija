---
export const prerender = false;

import BlogIndexPage from "@/components/pages/BlogIndexPage.astro";
import { blogService } from "@/features/blog/services/blog.service";
import { pageContentService } from "@/features/page-content/services/page-content.service";
import BaseLayout from "@/components/components/BaseLayout.astro";

const content = await pageContentService.getBlogIndexPageData();
const posts = await blogService.listPublishedPosts();
---

<BaseLayout
  title={content.seo.title}
  description={content.seo.description}
  canonicalPath="/blog/"
  appBarTone="dark"
  appBarOverlay={true}
>
  <BlogIndexPage posts={posts} content={content} />
</BaseLayout>
